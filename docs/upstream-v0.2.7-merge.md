# v0.2.7 上游合并记录

日期：2026-09-20。目标：重叠功能采用上游实现，保留独立二次开发功能。

## 基线与范围

- 当前分支：`codex/my`。
- 合并前：`63aebd3b04c1a5db4cd8b36443ca36ebfa7375f5`。
- 共同基线：v0.2.3，`8fa67d477d6651a744754392a8982ea589c26ae6`。
- 合并目标：`Wei-Shaw/sub2api` 的 v0.2.7，`aea725f2ea644d5592d0bbb1d63b607efa7e200a`。
- 保留备份分支：`codex/backup-before-v0.2.7-20260920`。
- 通过 Git 完整合并目标历史，纳入 338 个尚未包含的上游提交；没有按功能挑选提交。
- 上游相对共同基线改动 665 个路径，与本地改动重叠 141 个路径。26 个文本冲突文件均逐项处理；目标版本跟踪的文件无缺失。
- 上游标签中的 `backend/cmd/server/VERSION` 仍为 0.2.5，本次校正为 0.2.7。

## 功能与冲突处理

| 范围 | 合并结果 |
| --- | --- |
| 上游新增能力 | 纳入 Seedance 原生任务创建、查询、删除与 usage 计费，插件宿主 KV/账号目录及只读状态通道，MiniMax/OpenCode Go 平台，订阅批量操作等完整代码与测试。 |
| 上游修复 | 保留 Gemini 裸模型名/thinking 转换、Go/Python GenAI SSE 兼容、Kimi 配额限时暂停、Codex 工具 schema、DeepSeek 思考回退等修复。 |
| 高峰倍率 | 采用上游规则：仅订阅分组、当日时间段、基础 token 倍率乘高峰因子；前端表单、模型显示及画布报价同步，图片按次倍率不叠加高峰因子。 |
| 视频与图片 | 原生 Seedance 使用上游协议与计费；保留本地 OpenAI 兼容视频、Veo、Grok 扩展及图片实际尺寸识别。渠道视频定价表单采用上游共用组件，模型广场保留分组视频覆盖价。 |
| API Key | 保留本地同平台多分组与计费优先级；补齐新平台校验、选择及批量编辑字段，避免平台被错误归一为 Anthropic。余额查询保持上游允许查询的行为。 |
| 多订阅 | 保留按具体订阅 ID 选择、扣费和失效缓存；纳入上游订阅批量操作与延长时长的事务锁。周期到期判断与重置时间采用上游规则，移除本地到期边界额外延后一分钟的显示。 |
| 支付履约 | 写入本地订单订阅 ID 时使用上游租约条件，并保留租约时间戳；过期 worker 无法写入关联。已有历史成功审计的订单不会重复发放订阅。保留优惠券核销及返利。 |
| 站点与页面 | 保留定制首页、运营分析、Playground、无限画布、营销抽奖、优惠券、自定义菜单/页脚/客服及访问限制；公开设置、注入载荷、类型和页面同时合并。定制首页套餐入口遵守上游订阅开关。 |
| 部署与插件 | 保留本地部署配置并纳入上游配置项；Windows 插件 ZIP 释放句柄流程采用上游实现。 |

## 数据迁移

上游新增迁移保持原文件名和内容：`237_add_minimax_platform.sql`、`238_opencode_go_platform.sql`、`238_purge_unlimited_user_platform_quotas.sql`。迁移器以完整文件名跟踪，两个不同名称的 238 文件均保留。

新增 `239_custom_api_key_platform_sync.sql`：将未删除、主分组属于新平台、但历史 `platform` 被写为 `anthropic` 的 API Key 同步到主分组平台。不修改既有迁移的校验和。

本次仅修改源码及构建产物，未启动新服务执行数据库迁移。部署前应备份数据库；备份分支只能回退代码，不能撤销已经执行的数据迁移。

## 验证

- 前端完整测试：322 个文件、2351 项测试通过。
- 最后调整订阅页面后，相关 4 个测试文件、28 项测试通过，并重新构建。
- 后端 `go test ./...` 与 `go test -tags=unit ./...` 全部通过。
- 前端 `pnpm run build` 通过，包含国际化键检查和 TypeScript 编译；仅有资源包大小提示。
- 后端 `go build -tags embed -trimpath -o ../.runtime/sub2api-v0.2.7-verify.exe ./cmd/server` 通过；`-version` 输出 `Sub2API 0.2.7`。
- `git diff --cached --check` 通过，冲突索引条目为 0。
- SSE 心跳用例改为等待实际心跳事件，连续 5 次复测通过；Ollama 测试使用确定不同的时间戳，避免 Windows 时钟精度导致代际相同。
- 增加/更新新平台 API Key、多分组选择、租约失效拒绝、旧订单恢复及订阅周期边界回归测试。

验证范围为本地源码、自动化测试和构建；未进行真实支付、外部模型调用、生产数据库迁移或生产部署验收。
