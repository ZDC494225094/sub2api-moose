# 充值活动

后台入口：订单管理 / 充值活动，路由 `/admin/orders/campaigns`。

## 结算规则

- 仅支持余额充值。已启用且在有效期内的活动对所有用户自动生效，无需手选。按充值金额匹配门槛；多个活动重叠时，按每元实付可获得的余额择优（按活动比例比较），优惠相同取最新活动。一次订单仅享一个活动，不叠加优惠券。后端对未传活动 ID 的订单也自动匹配。
- 顶部导航下方滚动展示正在进行的活动，点击进入充值页。快捷金额直接展示活动徽标：折扣显示含现有手续费的实付金额，赠送显示换算后的到账余额；未达门槛仍可标准充值。页面每分钟刷新活动，并实时处理已知活动的起止时间。
- 赠送 10%：充值 50 到账 55，充值 100 到账 110。
- 实付比例 90%：充值 100 实付 90，到账 100。
- 以上示例以余额换算倍率 1、手续费 0 为前提，现有换算倍率和手续费继续适用。
- 起止时间使用带时区的时间戳，开始包含、结束不包含。定点活动用开始时刻和持续窗口配置。
- 按下单时间确定资格，活动结束前已创建的订单仍使用原支付超时和回调恢复规则。
- 活动规则保存在订单快照中。规则版本变化时，新下单必须刷新确认；已创建订单不受后续修改影响。

## 邀请奖励

- 使用已有邀请码与唯一邀请绑定关系，仅一级奖励。可选择只奖励活动期间新绑定的用户。
- 奖励基数为折扣后的充值本金换算所得余额，不包括赠送余额和手续费。
- 每单有奖励上限和冻结时长。建议起步比例 3%–5%、冻结 72 小时，根据实际毛利调整。
- 活动邀请规则替代该订单的常规返利；活动比例为 0 时，该活动订单不产生邀请返利。
- 需先在系统设置启用邀请返利。默认新活动停用、邀请奖励为 0。
- 支付成功后复用已有返利账本和幂等审计；返利可通过邀请返利页转入余额。
- 退款按退回到账余额的比例追回奖励，原奖励流水保留，新增负数退款流水。已转入余额的奖励形成返利欠额，抵扣后续返利。
- 历史邀请没有可靠绑定时间，不会被当作新活动邀请；可关闭“仅新绑定”覆盖既有关系。

## 分享

后台可导出 900 × 1600 PNG 卡片或复制通用链接。卡片包含站点名称和 Logo、平台介绍、首页广告语、同款点阵地球，以及 OpenAI、Claude、Gemini、DeepSeek、Qwen、Grok 标识。自定义远程 Logo 需允许跨域读取，否则使用默认 Logo 并提示。用户在充值页分享时，奖励活动附带本人的邀请码。二维码指向公开活动页，支持匿名浏览及登录后参与。

## 部署与验证

用户分享卡片额外展示用户名称与圆形头像（900 × 1720）；头像加载失败时使用名称首字。名称和头像仅绘入卡片，不拼接到分享链接中。管理员通用卡片保持 900 × 1600。导出前等待地球的站点 Logo 贴图加载完成；扫码落地页沿用卡片的浅绿品牌样式、点阵地球与厂商标识。

随新后端应用 `240_recharge_campaigns.sql`，增加活动表及邀请绑定时间列。部署前备份 `user_affiliates` 与现有业务数据库，部署后检查活动接口和后台菜单，再启用活动。无需修改历史订单数据。

回滚应用时保留新增表和列即可；已创建活动订单需继续由支持活动快照的后端完成结算与退款，不能在存在待支付活动订单时直接回退旧后端。

定向测试：

```powershell
cd backend
go test -tags unit ./internal/service ./internal/handler ./internal/repository -run 'Test(RechargeCampaign|.*Refund|.*Affiliate|.*WeChatPayment)' -count=1
```

PostgreSQL 事务测试仅接受数据库名 `campaign_qa`，内部创建独立 schema 并在完成后删除。设置 `CAMPAIGN_TEST_DSN` 后执行 `go test -tags unit ./internal/service -run TestRechargeCampaignPostgres -v -count=1`。

前端测试包含金额预览、下单传递活动版本和活动时间/门槛禁用状态。真实支付回调和真实微信授权需在部署环境使用支付渠道测试订单验证。
